module.exports = {
    config: {
        name: "spam",
        aliases: ["sp"],
        version: "1.5",
        credit: "MOHAMMAD BADOL",
        role: 1,
        cooldown: 5, 
        description: "Spam a beautifully designed long-space layout.",
        category: "owner"
    },

    onStartBadol: async function (api, event, args) {
        const HARDCODED_OWNER_ID = "61591265887748";
        if (event.senderID !== HARDCODED_OWNER_ID) {
            return api.sendMessage("⛔ Access Denied: This command can only be used by the bot owner!", event.threadID, event.messageID);
        }

        // সংখ্যা উল্লেখ না করলে এখন ডিফল্ট হিসেবে ১ বার স্প্যাম হবে
        const count = parseInt(args[0]) || 1;

        if (isNaN(count) || count <= 0) {
            return api.sendMessage("❌ Please provide a valid number of counts!", event.threadID, event.messageID);
        }

        // আপনার লং স্পেস লেআউটটিকে আরও প্রিমিয়াম ও সুন্দর ডিজাইনে সাজানো হয়েছে
        const spamMessage = `🔻















































































































































































































































































































































































🔹 ▪️ ▪️ 🔹












































































































































































































































































































































































▪️















































































































































































































































































































































































🔹 ▪️ ▪️ 🔹















































































































































































































































































































































































▫️`;

        // যদি ১ বারের বেশি হয়, তবে একটি স্টার্টিং মেসেজ দেখাবে
        if (count > 1) {
            api.sendMessage(`⏳ Starting spam for ${count} times...`, event.threadID);
        }

        // লুপের মাধ্যমে মেসেজ পাঠানোর সিস্টেম (প্রতি মেসেজে ১.৫ সেকেন্ড গ্যাপ)
        for (let i = 0; i < count; i++) {
            setTimeout(() => {
                api.sendMessage(spamMessage, event.threadID);
            }, i * 1500);
        }
    }
};

